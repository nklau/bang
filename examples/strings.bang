const name = "John Smith"
name = "Sally" // TODO: this will be a semantic error after context checks have been implemented

firstName = "Ray"
lastName = "Toal"
combinedName = firstName + " " + lastName
interpolatedName = $'{firstName} {lastName}'

print("//")
print(/* */)
print(/* // */)

// syntax errors
print("\/*")
print(/*) */
print(//)